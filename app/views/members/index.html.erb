<div class="container">
  <div class="row">
    <div class="col-xs-9">
      <h2>メンバー一覧</h2>
      <table>
        <tr>
          <th>名前</th>
          <th>メールアドレス</th>
        </tr>
        <% @members.each do |member| %>
        <tr>
          <td><%= member.last_name %><%= member.first_name %></td>
          <td><%= member.email %></td>
        </tr>
        <% end %>
      </table>

    </div>
  </div>
</div>
